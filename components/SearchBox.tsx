'use client';
import { useRouter } from 'next/navigation';
import { FormEvent, useState } from 'react';
export function SearchBox({ compact=false }: { compact?: boolean }) { const [q,setQ]=useState(''); const [loading,setLoading]=useState(false); const router=useRouter(); function submit(e:FormEvent){e.preventDefault(); if(!q.trim()) return; setLoading(true); setTimeout(()=>router.push(`/search?q=${encodeURIComponent(q.trim())}`), 350)} return <form onSubmit={submit} className={`archive-card rounded-3xl p-2 ${compact?'':'mx-auto max-w-4xl'}`}><div className="flex flex-col gap-3 md:flex-row"><input value={q} onChange={(e:any)=>setQ(e.target.value)} className="min-h-14 flex-1 rounded-2xl border border-gold/20 bg-vellum/95 px-5 text-ink outline-none placeholder:text-ink/50" placeholder="Search Abbasid Caliphate, Ibn Battuta, Al-Andalus, Mongols, Ottoman Empire, Indus Valley…"/><button className="rounded-2xl bg-gold px-7 py-4 font-semibold text-archive transition hover:bg-vellum">{loading?'Opening archive…':'Enter the Archive'}</button></div>{loading && <div className="mt-3 h-1 overflow-hidden rounded-full bg-vellum/10"><div className="h-full w-1/2 animate-pulse bg-gold" /></div>}</form> }
