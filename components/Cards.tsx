import Link from 'next/link';
import { Book, Topic } from '@/lib/data';
export function Section({ title, eyebrow, children }: { title:string; eyebrow?:string; children:React.ReactNode }) { return <section className="mx-auto max-w-7xl px-4 py-12"><p className="mb-2 text-sm uppercase tracking-[.35em] text-gold">{eyebrow}</p><h2 className="mb-6 font-serif text-3xl text-vellum md:text-5xl">{title}</h2>{children}</section> }
export function BookCard({ book }: { book: Book }) { return <Link href={`/books/${book.id}`} className="archive-card group rounded-3xl p-4 transition hover:-translate-y-1 hover:border-gold/60"><img src={book.coverImage} alt="" className="mb-4 h-56 w-full rounded-2xl object-cover"/><div className="flex items-center justify-between gap-2"><span className="rounded-full bg-gold/15 px-3 py-1 text-xs text-gold">{book.language}</span>{book.metadataOnly && <span className="rounded-full bg-vellum/10 px-3 py-1 text-xs text-vellum/70">metadata only</span>}</div><h3 className="mt-3 font-serif text-xl text-vellum group-hover:text-gold">{book.title}</h3><p className="mt-1 text-sm text-vellum/65">{book.author} • {book.era}</p><p className="mt-3 line-clamp-3 text-sm text-vellum/70">{book.description}</p></Link> }
export function TopicCard({ topic }: { topic: Topic }) { return <Link href={`/topic/${topic.slug}`} className="archive-card rounded-3xl p-5 transition hover:border-gold/60"><p className="text-sm text-gold">{topic.type}</p><h3 className="mt-2 font-serif text-2xl text-vellum">{topic.title}</h3><p className="mt-2 text-sm text-vellum/70">{topic.summary}</p><div className="mt-4 flex flex-wrap gap-2">{topic.alternateNames.slice(0,3).map(a=><span key={a} className="rounded-full border border-vellum/10 px-3 py-1 text-xs text-vellum/60">{a}</span>)}</div></Link> }
